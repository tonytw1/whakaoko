<p><h4>whakaoko</h4>
(verb) to listen to.</p>

<h5>Subscriptions / $subscription.name #if($subscriptionSize > 0) ($subscriptionSize) #end</h5>

<p>$subscription</p>

<a href="/subscriptions/$subscription.id/rss">RSS</a>

<p><a href="/subscriptions/$subscription.id/delete">Delete</a></p>

#if($feedItems && !$feedItems.isEmpty()) 
	<ul>
 	#foreach($feedItem in $feedItems)
 		<li><a href="$feedItem.url">$!feedItem.title</a>
 		<br/>$feedItem.date
 		</li>
 	#end
 	</ul>
#else
	<p>No items from this subscription.</p>
#end