<p><h4>whakaoko</h4>
(verb) to listen to.</p>

<h5>Subscriptions</h5>
#if($subscriptions && !$subscriptions.isEmpty()) 
	<ul>
 	#foreach($subscription in $subscriptions) 
 		<li>$!subscription.name</li>
 	#end
 	</ul>
#else
	<p>No current subscriptions.</p>
#end


<h5>Inbox ($inboxSize)</h5> 
#if($inbox && !$inbox.isEmpty()) 
	<ul>
 	#foreach($inboxItem in $inbox)
 		<li><a href="$inboxItem.url">$!inboxItem.title</a>
 		<br/>$inboxItem.date
 		</li>
 	#end
 	</ul>
#else
	<p>Inbox currently empty.</p>
#end