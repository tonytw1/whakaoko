<p><h4>whakaoko - $currentUserService.currentUser</h4>
(verb) to listen to.</p>

<h5>Channels</h5>

<p><a href="$urlBuilder.getNewChannelUrl($currentUserService.currentUser)">New channel</a></p>

<p><a href="/ui/$currentUserService.currentUser/subscriptions/new">New subscription</a></p>

#if($channels && !$channels.isEmpty()) 
	<ul>
 	#foreach($channel in $channels) 
 		<li><a href="$urlBuilder.getChannelUrl($channel)">$!channel.name</a>
 		</li>
 	#end
 	</ul>
#else
	<p>No channels.</p>
#end

<hr/>
<h5>Credentials</h5>
<p>Linked Instagram account: $instagramCredentials - <a href="/instagram/authorise/$currentUserService.currentUser">Link</a></p>
<p>Linked Twitter account: $twitterCredentials - <a href="/twitter/authorise/$currentUserService.currentUser">Link</a></p>
<hr/>